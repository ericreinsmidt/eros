# EROS settings. This file is the whole settings UI.
# bt_mac=AA:BB:CC:DD:EE:FF   auto-connect this Bluetooth sink at boot
# volume: 0..100, applied at boot. 40 -> 8/20.
volume=40
# brightness=6               0..10, applied at boot
startup_system=Muse          start on this card (matches a card name)
